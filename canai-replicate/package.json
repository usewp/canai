{
  "name": "replica",
  "version": "3.3.2",
  "description": "Replicate a live website as semantic single-HTML-per-page files for canai-prepare → WPCanAI. Includes page mode for high-fidelity single-URL replication.",
  "type": "module",
  "private": true,
  "bin": {
    "replica": "./bin/replica"
  },
  "scripts": {
    "start": "./bin/replica",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
